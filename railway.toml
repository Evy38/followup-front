# Configuration Railway - Frontend Angular SSR
# Railway lit ce fichier pour savoir comment construire et démarrer le service.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
